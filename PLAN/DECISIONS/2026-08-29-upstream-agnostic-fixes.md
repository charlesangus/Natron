# Upstream what's platform-agnostic

2026-08-29. Open matching PRs against `NatronGitHub/Natron` for fixes that
aren't Linux/Qt6/VFX-Platform-specific (e.g. the OpenEXR bump) — keeps a
bridge open with `lockewerks`/`cedricp`'s work rather than letting the fork
fully diverge.
