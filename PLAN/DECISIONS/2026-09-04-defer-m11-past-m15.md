# Defer M11 (plugin integration test) past M15

2026-09-04. M11's original scope — plugin loading — is already passing (28/28
ctest cases). The remaining scope — render-through-plugins golden-image testing
and video I/O (which needs ffmpeg) — is deferred past M15 (release packaging).
M15 ships without M11 as a gate. M11 stays on the board as a hardening pass
after packaging lands.
